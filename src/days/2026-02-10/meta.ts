import { defineDay } from "../../lib/day";

export default defineDay({
  title: "I Decided To Go Running",
  description:
    "A one-button run where every outcome is a loss: hazards, collapse, or the bed at the finish line.",
  tags: ["game", "runner", "jump", "exhaustion", "night"],
  shellMode: "immersive",
});
