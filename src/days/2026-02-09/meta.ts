import { defineDay } from "../../lib/day";

export default defineDay({
  title: "System Status (Monday)",
  description:
    "A status board that degrades every Monday, then publishes a sanitized postmortem.",
  tags: ["monday", "incident", "status", "corporate", "cynical"],
  shellMode: "default",
});
