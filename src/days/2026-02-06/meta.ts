import { defineDay } from "../../lib/day";

export default defineDay({
  title: "DEVOPS MATCH // SIGNAL -> ACTION",
  description:
    "Match the incident signal to the calm response. A quiet matching game.",
  tags: ["game", "matching", "devops", "calm", "minimal"],
});
