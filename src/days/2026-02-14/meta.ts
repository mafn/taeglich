import { defineDay } from "../../lib/day";

export default defineDay({
  title: "Will you be my Valentine?",
  description: "A classic question with a slightly persistent interface.",
  tags: ["interactive", "valentine", "toy", "chaos"],
  shellMode: "compact",
});
