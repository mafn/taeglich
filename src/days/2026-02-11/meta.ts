import { defineDay } from "../../lib/day";

export default defineDay({
  title: "Still Running",
  description:
    "The day after the run: a static snapshot and a link back to yesterday's race.",
  tags: ["daily", "image", "runner", "follow-up"],
  shellMode: "compact",
});
