import { defineDay } from "../../lib/day";

export default defineDay({
  title: "31.1. Tiny Boot: Panic",
  description:
    "A tiny boot console. Fix the filesystem. Or don’t. The end is the same.",
  tags: ["terminal", "boot", "panic", "toy"],
});
