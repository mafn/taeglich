import { defineDay } from "../../lib/day";

export default defineDay({
  title: "No Backfill",
  description:
    "One hundred and forty-five empty days, followed by one present square.",
  tags: ["daily", "return", "minimal", "generative"],
  shellMode: "immersive",
  renderH1: false,
});
